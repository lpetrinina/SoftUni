<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">

        <title>{{#if pageTitle}}{{pageTitle}}{{else}} Power of Nature {{/if}}</title>
        <link rel="stylesheet" href="/css/styles.css">
    </head>

    <body>
        <header>

            <nav>
                <ul>
                    {{!-- all users --}}
                    <li><a href="/">Home</a></li>
                    <li><a href="/disasters/search">Search</a></li>
                    <li><a href="/disasters/catalog">Power of Nature</a></li>
                    {{#if user}}
                    {{!-- logged users --}}
                    <li><a href="/disasters/create">Create Event</a></li>
                    <li><a href="/auth/logout">Logout</a></li>
                    {{else}}
                    {{!-- guest users --}}
                    <li><a href="/auth/login">Login</a></li>
                    <li><a href="/auth/register">Register</a></li>
                    {{/if}}
                </ul>
            </nav>

        </header>

        <main>
            <!-- In case of error, you should display div with class "errorContainer" -->
            <!-- You can choose to display the first error or all of them (error message/s in your opinion) -->

            {{#if error}}
            <div>
                <div class="errorContainer">
                    <p>{{error}}</p>
                </div>
            </div>
            {{/if}}

            {{{body}}}

        </main>

        <footer>
            <p>&copy JS Back-End 2025</p>
        </footer>
    </body>

</html>